/**
 * 
 */
document.addEventListener('DOMContentLoaded', () => {
	
	/*window.setTimeout(getClientList,200)
	
	function getClientList(){
		fetch("/stocks/clientList")
		.then(result => result.json())
		.then(result => {result})
	}
	*/
}) //End Point
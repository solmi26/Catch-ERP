<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cherp.app.common.myPage.mapper.MyPageMapper">
	
	<select id="selectEmployeeInfo" parameterType="String" resultType="employeeVO">
		SELECT e.name, ed.employee_image
		FROM   employees e 
		  JOIN employees_detail ed ON(e.employee_code = ed.employee_code) 
		WHERE  e.employee_id = #{employeeId}
	</select>
</mapper>